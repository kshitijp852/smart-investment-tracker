
Client notes:
- This demo uses raw canvas drawing for a tiny chart to avoid adding Chart.js as a dependency in the starters.
- For production, install chart.js: `npm install chart.js` and integrate React Chart.js wrappers like `react-chartjs-2`.
